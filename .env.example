# Vercel / production checklist. Do not put real secrets in source control.
# Create a hosted Postgres (Supabase) database, then set these in the Vercel project.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
APP_URL="https://your-vercel-app.vercel.app"
SESSION_SECRET="long-random-secret"
CRM_IMPORT_API_KEY="long-random-zapier-secret"
# Optional. Lead analysis + Agente pipeline phrasing. Blank = rule-based / keyword fallback (still uses live leads).
OPENAI_API_KEY=""
