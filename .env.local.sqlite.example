# Local SQLite development. Copy to `.env` in the repo root.

DATABASE_URL="file:./dev.db"
APP_URL="http://localhost:3000"
SESSION_SECRET="replace-with-a-long-random-secret"
CRM_IMPORT_API_KEY="replace-with-a-zapier-webhook-secret"
# Optional. Lead analysis + Agente pipeline phrasing. Blank = rule-based answers over Prisma leads.
OPENAI_API_KEY=""
